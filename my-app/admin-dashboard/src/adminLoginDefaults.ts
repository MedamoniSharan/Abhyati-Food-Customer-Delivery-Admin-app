/**
 * Default admin sign-in values for the dashboard login form.
 * Keep in sync with `my-app/backend/src/config/env.js` (ADMIN_EMAIL / ADMIN_PASSWORD `.default(...)`)
 * and optional overrides in `my-app/backend/.env`.
 */
export const ADMIN_DEFAULT_EMAIL = 'admin@abhyati.com'
export const ADMIN_DEFAULT_PASSWORD = 'admin'
