# Optional: override backend origin. Dev default is in `.env.development` (localhost:3001).
# VITE_API_BASE_URL=http://localhost:4000

# Optional: Google Maps Embed API key for in-app route preview.
# VITE_GOOGLE_MAPS_API_KEY=
